import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.gormlabenz.capacitoronesignal',
  appName: 'CapOne',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
